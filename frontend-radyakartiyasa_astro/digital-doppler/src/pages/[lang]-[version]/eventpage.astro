---
import Acarapage from '../eventpage.astro';
export function getStaticPaths () {
return [
    {params: {lang: 'id', version: 'v1'}},
    {params: {lang: 'en', version: 'v1'}},
  ];
}

const { lang, version } = Astro.params;
---

<Acarapage/>
